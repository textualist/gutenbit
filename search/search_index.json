{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Home","text":"gutenbit gutenbit gutenbit gutenbit <p>A command line tool for fast local search across public-domain literary works. Find, browse, and search books from your terminal or Python script.</p>"},{"location":"#cli-install","title":"CLI Install","text":"<p>Try the latest stable release from PyPI without a persistent install:</p> <pre><code>uvx gutenbit --help\n</code></pre> <p>Or install it like this and then run <code>gutenbit --help</code>:</p> <pre><code>uv tool install gutenbit\n</code></pre> <p>gutenbit stores its database and catalog cache in a <code>.gutenbit/</code> folder.</p>"},{"location":"#cli-example","title":"CLI Example","text":"<p>Find a book in the Project Gutenberg catalog and download it locally. Sections are parsed automatically during import.</p> <pre><code>gutenbit catalog --author \"Austen, Jane\"\ngutenbit add 1342\n</code></pre> <p>Inspect the table of contents, read the opening, or jump into a section.</p> <pre><code>gutenbit toc 1342\ngutenbit view 1342\ngutenbit view 1342 --section 1 --forward 5\n</code></pre> <p>Search within the book and read exact matches or nearby context.</p> <pre><code>gutenbit search \"truth universally acknowledged\" --book 1342 --phrase\ngutenbit search \"bennet\" --book 1342 --limit 3 --radius 1\n</code></pre> <p>All commands support <code>--json</code> for machine-readable output. CLI-managed state is stored under <code>.gutenbit/</code> by default, including the database at <code>.gutenbit/gutenbit.db</code> and the catalog cache under <code>.gutenbit/cache/</code>.</p>"},{"location":"#python","title":"Python","text":"<p>gutenbit can also be used as a python module. Add it to your project with:</p> <pre><code>uv add gutenbit\n</code></pre> <pre><code>from gutenbit import Catalog, Database\n\ncatalog = Catalog.fetch()\nbook = catalog.get(1342)\n\nif book is not None:\n    with Database(\".gutenbit/gutenbit.db\") as db:\n        db.ingest([book])\n        for hit in db.search(\"truth universally acknowledged\", book_id=1342):\n            print(hit.title, hit.div1, hit.content[:80])\n</code></pre>"},{"location":"#next-steps","title":"Next steps","text":"<ul> <li>Getting Started walks through a complete workflow.</li> <li>CLI documents every subcommand and flag.</li> <li>Python API covers the library in full.</li> <li>Concepts explains how chunking, divisions, and search work.</li> <li>API Reference has auto-generated module documentation.</li> </ul>"},{"location":"#project-gutenberg-access","title":"Project Gutenberg Access","text":"<p>gutenbit is an open-source project not affiliated with Project Gutenberg. It is for individual downloads, not bulk downloading. It prefers official mirrors and uses the main site only as a zip fallback, with a default <code>2.0</code> second delay between downloads. gutenbit also sends an identifying default <code>User-Agent</code> on Gutenberg and PGLAF requests: <code>gutenbit/&lt;version&gt; (+https://gutenbit.textualist.org)</code>. Review Project Gutenberg's Robot Access Policy and Terms of Use.</p>"},{"location":"cli/","title":"CLI","text":"<p>The <code>gutenbit</code> command-line tool provides seven subcommands that follow a natural workflow: find books, add them, explore their structure, read text, and search.</p> <p>Start with <code>gutenbit --help</code> for the workflow overview, then use <code>gutenbit COMMAND --help</code> for command-specific flags and examples.</p>"},{"location":"cli/#installation","title":"Installation","text":"<p>Try the latest stable release from PyPI without a persistent install:</p> <pre><code>uvx gutenbit --help\n</code></pre> <p>Or install it like this and then run <code>gutenbit --help</code>:</p> <pre><code>uv tool install gutenbit\n</code></pre> <p>gutenbit stores its database and catalog cache in a <code>.gutenbit/</code> folder.</p> <p>All CLI-managed state lives under <code>.gutenbit/</code> by default: the database is <code>.gutenbit/gutenbit.db</code>, and the catalog cache is stored under <code>.gutenbit/cache/</code>. Use <code>--db PATH</code> to store the database elsewhere. All commands support <code>--json</code> for machine-readable output.</p>"},{"location":"cli/#project-gutenberg-access","title":"Project Gutenberg Access","text":"<p>gutenbit is an open-source project not affiliated with Project Gutenberg. It is for individual downloads, not bulk downloading. It prefers official mirrors and uses the main site only as a zip fallback, with a default <code>2.0</code> second delay between downloads. gutenbit also sends an identifying default <code>User-Agent</code> on Gutenberg and PGLAF requests: <code>gutenbit/&lt;version&gt; (+https://gutenbit.textualist.org)</code>. Review the Robot Access Policy and Terms of Use.</p>"},{"location":"cli/#catalog","title":"catalog","text":"<p>Search the Project Gutenberg catalog for books by metadata.</p> <pre><code>gutenbit catalog --author \"Dickens\"\ngutenbit catalog --title \"Christmas\" --author \"Dickens\"\ngutenbit catalog --author \"Dickens\" --refresh\ngutenbit catalog --subject \"Philosophy\" --limit 50\n</code></pre> Flag Description <code>--author TEXT</code> Filter by author (substring match) <code>--title TEXT</code> Filter by title (substring match) <code>--subject TEXT</code> Filter by subject (substring match) <code>--language CODE</code> Filter by language code (e.g. <code>en</code>) <code>--limit N</code> Maximum results (default: 20) <code>--refresh</code> Ignore the local catalog cache and redownload it now <code>--json</code> Output as JSON <p>Filters combine with AND logic. All matching is case-insensitive. The catalog is cached locally for two hours, filtered to English text records, and can be forced to redownload with <code>--refresh</code>.</p>"},{"location":"cli/#add","title":"add","text":"<p>Download books from Project Gutenberg and store them in the database.</p> <pre><code>gutenbit add 1342\ngutenbit add 46 730 967\ngutenbit add 1342 --refresh\ngutenbit add 2600 --delay 2.0\n</code></pre> Flag Description <code>BOOK_IDS</code> One or more Project Gutenberg IDs (positional) <code>--delay SECONDS</code> Pause between downloads (default: 2.0) <code>--refresh</code> Ignore the local catalog cache, redownload it now, and reprocess matching stored books <code>--json</code> Output as JSON <p>Books already stored at the current chunker version are skipped unless you pass <code>--refresh</code>, which also refreshes the catalog cache before reprocessing the requested book IDs. IDs that map to a different canonical edition are remapped automatically.</p>"},{"location":"cli/#books","title":"books","text":"<p>List all books stored in the database, or update stored books whose parser version is stale.</p> <pre><code>gutenbit books\ngutenbit books --json\ngutenbit books --update\ngutenbit books --update --force\ngutenbit books --update --dry-run\n</code></pre> Flag Description <code>--update</code> Reprocess stored books whose parser version is stale <code>--delay SECONDS</code> Pause between downloads in update mode (default: 2.0) <code>--force</code> Reprocess all stored books in update mode, even if already current <code>--dry-run</code> Show which stored books would be updated without downloading <code>--json</code> Output as JSON <p>Without <code>--update</code>, <code>books</code> behaves exactly as before and just lists stored books. With <code>--update</code>, gutenbit checks the local database and reprocesses only books whose stored text is out of date for the current chunker version. <code>--force</code> refreshes every stored book, and <code>--dry-run</code> reports what would be refreshed without doing any work.</p>"},{"location":"cli/#remove","title":"remove","text":"<p>Remove books and their chunks from the database.</p> <pre><code>gutenbit remove 1342\ngutenbit remove 46 730 967\n</code></pre> Flag Description <code>BOOK_IDS</code> One or more Project Gutenberg IDs (positional) <code>--json</code> Output as JSON <p>Exits with code 1 if any requested ID was not found.</p>"},{"location":"cli/#search","title":"search","text":"<p>Full-text search across stored books using SQLite FTS5 with BM25 ranking. Search targets text chunks by default.</p> <pre><code>gutenbit search \"bennet\"\ngutenbit search \"don't stop\"                              # punctuation is ok\ngutenbit search \"truth universally acknowledged\" --phrase\ngutenbit search \"ghost OR spirit\" --raw                   # FTS5 boolean query\ngutenbit search \"bennet\" --book 1342 --order first\ngutenbit search \"truth universally acknowledged\" --book 1342 --section 1 --phrase\ngutenbit search \"chapter\" --book 1342 --kind heading\ngutenbit search \"bennet\" --book 1342 --radius 1           # include surrounding passage\ngutenbit search \"bennet\" --book 1342 --limit 3\ngutenbit search \"bennet\" --book 1342 --count\n</code></pre> Flag Description <code>QUERY</code> Search query (positional) <code>--phrase</code> Treat query as an exact phrase (mutually exclusive with <code>--raw</code>) <code>--raw</code> Pass query directly to FTS5 for advanced syntax (mutually exclusive with <code>--phrase</code>) <code>--order ORDER</code> <code>rank</code> (default), <code>first</code>, or <code>last</code> <code>--author TEXT</code> Filter by author (substring match) <code>--title TEXT</code> Filter by title (substring match) <code>--book ID</code> Restrict to a single book <code>--kind KIND</code> Chunk kind to search: <code>text</code> (default), <code>heading</code>, or <code>all</code> <code>--section SELECTOR</code> Restrict to a section by path prefix or number from <code>toc</code> (number requires <code>--book</code>) <code>--limit N</code> Maximum results (default: 10) <code>--radius N</code> Surrounding passage to include on each side of each hit <code>--count</code> Just print the number of matches <code>--json</code> Output as JSON"},{"location":"cli/#query-modes","title":"Query modes","text":"<p>By default, punctuation in the query is auto-escaped so apostrophes, hyphens, and other punctuation just work. Tokens are implicitly AND'd.</p> <ul> <li>(default): Plain text \u2014 punctuation is auto-escaped, words are AND'd.</li> <li>--phrase: Exact phrase \u2014 word order and adjacency must match exactly.</li> <li>--raw: FTS5 syntax \u2014 AND, OR, NOT, NEAR(), prefix*, \"phrases\", (groups).</li> </ul>"},{"location":"cli/#search-order","title":"Search order","text":"<ul> <li>rank: Results ordered by BM25 relevance score, then book, then position.</li> <li>first: Earliest matches. Ordered by book ascending, then position ascending.</li> <li>last: Latest matches. Ordered by book descending, then position descending.</li> </ul>"},{"location":"cli/#result-shaping","title":"Result shaping","text":"<ul> <li>Use <code>--limit</code> to control how many hits are returned. The default is 10.</li> <li>Use <code>--radius</code> to read surrounding passage around each hit in normal reading order.</li> <li><code>--count</code> cannot be combined with <code>--radius</code>.</li> <li>Use <code>--kind heading</code> to search structural headings, or <code>--kind all</code> to include both headings and text.</li> </ul>"},{"location":"cli/#fts5-query-syntax","title":"FTS5 query syntax","text":"<p>When using <code>--raw</code>, the query is passed directly to SQLite FTS5. Supported syntax:</p> Syntax Meaning <code>war peace</code> Both terms (implicit AND) <code>war OR peace</code> Either term <code>war NOT peace</code> First term, excluding second <code>\"to be or not\"</code> Exact phrase <code>philos*</code> Prefix match <code>NEAR(war peace, 5)</code> Terms within 5 tokens of each other <code>(war OR battle) AND peace</code> Grouped boolean logic <p>Use <code>--phrase</code> to auto-wrap the entire query as an exact phrase without manual quoting.</p>"},{"location":"cli/#toc","title":"toc","text":"<p>Show the structural table of contents for a stored book, with numbered sections. By default the table shows two heading levels; use <code>--expand</code> to collapse further or reveal all nested levels.</p> <pre><code>gutenbit toc 1342\ngutenbit toc 100 --expand 1\ngutenbit toc 100 --expand all\ngutenbit toc 2600 --json\n</code></pre> Flag Description <code>BOOK_ID</code> Project Gutenberg book ID (positional) <code>--expand {1,2,3,4,all}</code> Show heading levels up to this depth (default: <code>2</code>; <code>all</code> shows every stored level) <code>--json</code> Output as JSON <p>Collapsed rows roll hidden descendants into the lowest shown level. For example, with <code>--expand 2</code>, visible act rows include the stats for their hidden scenes. Section numbers remain stable and can be passed to <code>view --section</code> or <code>search --section</code>.</p>"},{"location":"cli/#view","title":"view","text":"<p>Read stored book text. Starts at the first structural section by default. Use selectors to focus on a specific part.</p> <pre><code>gutenbit view 1342                              # first structural section\ngutenbit view 1342 --all                        # full book\ngutenbit view 1342 --section 1                  # section by number\ngutenbit view 1342 --section 1 --all            # full section, including nested subsections\ngutenbit view 1342 --section \"Chapter 1\" --forward 10  # section by path\ngutenbit view 100 --section \"ALL\u2019S WELL THAT ENDS WELL / ACT I\" --all  # full act incl. scenes\ngutenbit view 1342 --position 1 --forward 5           # from exact position\ngutenbit view 1342 --position 1 --radius 2     # surrounding passage around position\ngutenbit view 1342 --section 1 --radius 2       # surrounding passage around section start\n</code></pre> Flag Description <code>BOOK_ID</code> Project Gutenberg book ID (positional) <code>--section SELECTOR</code> Section number (from <code>toc</code>) or path prefix (e.g. <code>\"BOOK I/CHAPTER I\"</code>) <code>--position N</code> Exact chunk position <code>--all</code> Read the full selected scope (whole book or selected section, including nested subsections) <code>--forward N</code> Passages to read forward (default: 3 for opening, 1 for section/position) <code>--radius N</code> Surrounding passage to include on each side of the selected center passage <code>--json</code> Output as JSON <p>Use <code>--section</code> or <code>--position</code>, not both. <code>--forward</code>, <code>--radius</code>, and <code>--all</code> are mutually exclusive in <code>view</code>. Use <code>--all</code> for a whole book or selected section subtree; choosing a parent section such as a play or act includes its nested descendants. <code>--all</code> does not apply to <code>--position</code>. Run <code>toc</code> first to see available section numbers.</p>"},{"location":"cli/#json-output","title":"JSON output","text":"<p>Every command accepts <code>--json</code> and returns a unified envelope:</p> <pre><code>{\n  \"ok\": true,\n  \"command\": \"search\",\n  \"data\": { ... },\n  \"warnings\": [],\n  \"errors\": []\n}\n</code></pre> <p>When <code>ok</code> is <code>false</code>, the <code>errors</code> list contains error messages. The <code>data</code> field holds command-specific results. The <code>warnings</code> list captures non-fatal issues (e.g. a requested ID not found during bulk remove).</p> <p>For <code>view</code>, the response body is content-first. Successful responses include a shared passage shape: <code>book</code>, <code>title</code>, <code>author</code>, <code>section</code>, <code>section_number</code>, <code>position</code>, <code>forward</code>, <code>radius</code>, <code>all</code>, and <code>content</code>.</p> <p>For <code>search</code>, <code>data[\"order\"]</code> records the selected result order, <code>data[\"filters\"]</code> includes the resolved <code>kind</code>, and <code>data[\"items\"]</code> remains the hit list. Each hit uses that same passage shape, with search-specific fields such as <code>kind</code>, <code>rank</code>, and <code>score</code> appended after the shared fields. When <code>--radius</code> is used, <code>content</code> is the joined surrounding passage in reading order.</p>"},{"location":"cli/#global-flags","title":"Global flags","text":"<p>These flags apply to all subcommands:</p> Flag Description <code>--db PATH</code> SQLite database path (default: <code>.gutenbit/gutenbit.db</code>) <code>-v</code>, <code>--verbose</code> Enable debug logging"},{"location":"concepts/","title":"Concepts","text":"<p>This page explains the internal model that shapes gutenbit's output. Understanding these concepts makes search results, chunk metadata, and structural divisions easier to interpret.</p>"},{"location":"concepts/#pipeline","title":"Pipeline","text":"<p>gutenbit processes books in four stages:</p> <ol> <li> <p>Catalog. A CSV feed from Project Gutenberg provides metadata for every book: title, author, subjects, language, and Gutenberg ID. The catalog is filtered to English text and deduplicated so each work has one canonical ID.</p> </li> <li> <p>Download. Each book's HTML is fetched from official mirrors first, with a fallback to Gutenberg's epub-cache zip when needed.</p> </li> <li> <p>Chunk. The HTML is parsed into discrete text units. Each <code>&lt;p&gt;</code> element becomes one chunk. Structural metadata (divisions and kinds) is attached to every chunk.</p> </li> <li> <p>Store. Chunks, book metadata, and a reconstructed full text are written to a SQLite database. An FTS5 virtual table indexes all chunk content for full-text search.</p> </li> </ol>"},{"location":"concepts/#chunking","title":"Chunking","text":"<p>The chunker reads the book's HTML table of contents to build a structural map. Project Gutenberg HTML files include TOC links (<code>&lt;a class=\"pginternal\"&gt;</code>) that point to anchors on heading tags in the body. These links define where sections begin and what their headings are.</p> <p>Each <code>&lt;p&gt;</code> element between two section boundaries becomes its own chunk. Paragraphs are never merged or split. This preserves the exact paragraph structure of the original text.</p> <p>Content outside the Gutenberg <code>*** START ***</code> and <code>*** END ***</code> delimiters is excluded. If these delimiters are absent, the chunker falls back to Gutenberg's <code>pg-header</code> and <code>pg-footer</code> elements.</p> <p>When a book has no TOC links, the chunker falls back to scanning for heading tags (<code>&lt;h1&gt;</code> through <code>&lt;h3&gt;</code>) directly.</p>"},{"location":"concepts/#structural-divisions","title":"Structural divisions","text":"<p>Every chunk carries four division fields: <code>div1</code>, <code>div2</code>, <code>div3</code>, and <code>div4</code>. These represent a hierarchy from broadest to most specific.</p> <p>The heading level assigned to each section depends on the heading's content:</p> <ul> <li>Level 1 (broad): Headings with keywords like BOOK, PART, ACT, VOLUME, or EPILOGUE. Also headings marked bold in the TOC.</li> <li>Level 2 (chapter): Headings with CHAPTER, STAVE, LETTER, or similar keywords.</li> <li>Level 3 (sub-chapter): All other headings.</li> </ul>"},{"location":"concepts/#level-compaction","title":"Level compaction","text":"<p>Levels are compacted so the shallowest heading always fills <code>div1</code>. A book with only chapters puts them in <code>div1</code>. A book with both BOOK and CHAPTER headings puts books in <code>div1</code> and chapters in <code>div2</code>.</p> <p>Examples:</p> Book Structure div1 div2 Pride and Prejudice Chapters only <code>Chapter 1</code> (empty) War and Peace Books + Chapters <code>BOOK ONE: 1805</code> <code>CHAPTER I</code> Crime and Punishment Parts + Chapters <code>PART I</code> <code>CHAPTER I</code> <p>This compaction means <code>div1</code> always represents the broadest structural unit present in any given book, regardless of how many levels the book uses.</p>"},{"location":"concepts/#chunk-kinds","title":"Chunk kinds","text":"<p>Each chunk has a <code>kind</code> field:</p> Kind Meaning <code>heading</code> A section heading extracted from the TOC structure <code>text</code> A body paragraph within a section"},{"location":"concepts/#corpus-policy","title":"Corpus policy","text":"<p>gutenbit enforces a fixed corpus policy during catalog fetching:</p> <ul> <li>Language: English only (<code>en</code>).</li> <li>Media type: Text only.</li> <li>Deduplication: When multiple Gutenberg IDs correspond to the same work (same title and author), the lowest ID is kept as the canonical edition. All other IDs remap to it.</li> </ul> <p>The <code>canonical_id</code> method on <code>Catalog</code> resolves any Gutenberg ID to its canonical form. During <code>gutenbit add</code>, requested IDs are remapped automatically.</p>"},{"location":"concepts/#search","title":"Search","text":"<p>Search uses SQLite FTS5 with BM25 ranking. The index is configured with Porter stemming and unicode61 tokenization, which means:</p> <ul> <li>Stemming reduces words to roots. Searching for \"running\" also matches \"run\" and \"runs.\"</li> <li>Unicode normalization handles accented characters and non-ASCII text.</li> <li>Ranking considers term frequency, document length, and inverse document frequency across the corpus.</li> </ul> <p>Search results include the full text of each matching chunk along with its structural metadata, so you can identify where in a book a match occurs without a separate lookup. The CLI can also attach surrounding passage with <code>--radius</code> when you want local reading context around each hit.</p>"},{"location":"getting-started/","title":"Getting Started","text":"<p>This guide walks through a complete workflow: find a book, download it, explore its structure, and search its text. Both CLI and Python examples use Pride and Prejudice (Project Gutenberg ID 1342).</p>"},{"location":"getting-started/#installation","title":"Installation","text":"<p>Try the latest stable release from PyPI without a persistent install:</p> <pre><code>uvx gutenbit --help\n</code></pre> <p>Or install it like this and then run <code>gutenbit --help</code>:</p> <pre><code>uv tool install gutenbit\n</code></pre> <p>gutenbit stores its database and catalog cache in a <code>.gutenbit/</code> folder.</p>"},{"location":"getting-started/#cli-walkthrough","title":"CLI walkthrough","text":""},{"location":"getting-started/#find-a-book","title":"Find a book","text":"<p>Search the Project Gutenberg catalog by author, title, subject, or language:</p> <pre><code>gutenbit catalog --author \"Austen, Jane\"\n</code></pre> <pre><code>  Downloaded catalog from Project Gutenberg (English text corpus).\n      ID  AUTHORS                                   TITLE\n  ------  ----------------------------------------  -----\n    1342  Austen, Jane                              Pride and Prejudice\n     158  Austen, Jane                              Emma\n     161  Austen, Jane                              Sense and Sensibility\n     105  Austen, Jane                              Persuasion\n</code></pre>"},{"location":"getting-started/#download-and-store","title":"Download and store","text":"<p>Pass one or more Project Gutenberg IDs to <code>add</code>:</p> <pre><code>gutenbit add 1342\n</code></pre> <p>The book's HTML is downloaded, parsed into paragraph-level chunks with structural metadata, and stored in a local SQLite database (<code>.gutenbit/gutenbit.db</code> by default).</p>"},{"location":"getting-started/#explore-structure","title":"Explore structure","text":"<p>View the table of contents with numbered sections:</p> <pre><code>gutenbit toc 1342\n</code></pre> <p>By default, <code>toc</code> shows two heading levels. Use <code>--expand 1</code>, <code>--expand 3</code>, or <code>--expand all</code> to collapse further or reveal the full nested structure.</p> <pre><code>gutenbit toc 100 --expand all\n</code></pre> <p>Each section number can be used with <code>view --section</code> to jump directly to that part of the book. When deeper levels are collapsed, the visible lowest-level rows include the stats for the hidden descendants beneath them.</p>"},{"location":"getting-started/#read-text","title":"Read text","text":"<p>View the opening of the book:</p> <pre><code>gutenbit view 1342\n</code></pre> <p>Read a specific section:</p> <pre><code>gutenbit view 1342 --section 1 --forward 10\n</code></pre> <p>Read a full section:</p> <pre><code>gutenbit view 1342 --section 1 --all\n</code></pre> <p>If the selected section has nested subsections, <code>--all</code> includes the entire subtree. For example, selecting an act includes all of its scenes.</p> <p>Read from an exact chunk position:</p> <pre><code>gutenbit view 1342 --position 1 --forward 5\n</code></pre> <p>Read surrounding passage around a position or section start:</p> <pre><code>gutenbit view 1342 --position 1 --radius 2\ngutenbit view 1342 --section 1 --radius 2\n</code></pre> <p>Use <code>--forward</code> for forward reading, <code>--radius</code> for a surrounding passage window, and <code>--all</code> for a full book or selected section subtree. <code>--all</code> does not apply to <code>--position</code>.</p>"},{"location":"getting-started/#search","title":"Search","text":"<p>Full-text search across all stored books. Search targets text chunks by default:</p> <pre><code>gutenbit search \"pride\"\n</code></pre> <p>Search headings explicitly when needed:</p> <pre><code>gutenbit search \"chapter\" --book 1342 --kind heading\n</code></pre> <p>Narrow results to a single book:</p> <pre><code>gutenbit search \"pride\" --book 1342\n</code></pre> <p>Search for an exact phrase:</p> <pre><code>gutenbit search \"truth universally acknowledged\" --phrase\n</code></pre> <p>Search with nearby chunk context:</p> <pre><code>gutenbit search \"truth universally acknowledged\" --book 1342 --limit 3 --radius 1\n</code></pre> <p>All commands accept <code>--json</code> for machine-readable output.</p>"},{"location":"getting-started/#python-walkthrough","title":"Python walkthrough","text":""},{"location":"getting-started/#fetch-the-catalog","title":"Fetch the catalog","text":"<pre><code>from gutenbit import Catalog\n\ncatalog = Catalog.fetch()\nbooks = catalog.search(author=\"Austen, Jane\")\nfor book in books[:5]:\n    print(book.id, book.title)\n</code></pre> <p>The catalog is cached locally for two hours under <code>.gutenbit/cache/</code>, filtered to English text, and deduplicated by normalized title plus primary author, keeping the lowest Project Gutenberg ID as canonical. Use <code>--refresh</code> to force a redownload.</p>"},{"location":"getting-started/#ingest-books","title":"Ingest books","text":"<pre><code>from gutenbit import Database\n\nwith Database(\".gutenbit/gutenbit.db\") as db:\n    db.ingest(books[:3])\n</code></pre> <p><code>ingest</code> downloads each book's HTML, parses it into chunks, and stores everything in SQLite. Books already in the database are skipped.</p>"},{"location":"getting-started/#search_1","title":"Search","text":"<pre><code>results = db.search(\"pride\")\nfor hit in results:\n    print(f\"{hit.title} | {hit.div1} | {hit.content[:80]}\")\n</code></pre> <p>Results use BM25 rank ordering by default. Each <code>SearchResult</code> includes the matching text, its structural position (div1 through div4), book metadata, and a relevance score.</p>"},{"location":"getting-started/#read-structured-chunks","title":"Read structured chunks","text":"<pre><code># All chunks for a book\nchunks = db.chunk_records(1342)\n\n# Chunks in a specific section\nsection = db.chunks_by_div(1342, \"Chapter 1\")\n\n# A window of chunks around a position\nwindow = db.chunk_window(1342, position=50, around=2)\n</code></pre>"},{"location":"getting-started/#full-text","title":"Full text","text":"<pre><code>text = db.text(1342)\nprint(text[:500])\n</code></pre>"},{"location":"getting-started/#how-it-works","title":"How it works","text":"<p>The pipeline has four stages. The catalog provides book metadata and IDs. The downloader prefers official mirror HTML and falls back to the main site's HTML zip when needed. The chunker parses the HTML using its table of contents as a structural map, turning each paragraph into a discrete chunk with a position and a place in the book's heading hierarchy. The database stores chunks in SQLite with FTS5 indexing for fast full-text search with BM25 ranking.</p>"},{"location":"python-api/","title":"Python API","text":"<p>gutenbit exposes four public classes and one function from its top-level package.</p> <pre><code>from gutenbit import Catalog, BookRecord, Database, SearchResult, Chunk, chunk_html\n</code></pre>"},{"location":"python-api/#catalog","title":"Catalog","text":"<p><code>Catalog</code> fetches and searches the Project Gutenberg metadata catalog.</p>"},{"location":"python-api/#fetch","title":"Fetch","text":"<pre><code>from gutenbit import Catalog\n\ncatalog = Catalog.fetch()\n</code></pre> <p><code>fetch()</code> downloads the CSV catalog from Project Gutenberg, filters it to English text records, and deduplicates entries so each work maps to a single canonical ID (the lowest Gutenberg ID for that title/author pair). The result is a <code>Catalog</code> instance held in memory.</p>"},{"location":"python-api/#search","title":"Search","text":"<pre><code>results = catalog.search(author=\"Dickens\")\nresults = catalog.search(title=\"Christmas\", author=\"Dickens\")\nresults = catalog.search(subject=\"Philosophy\")\n</code></pre> <p>All filters use case-insensitive substring matching. When multiple filters are given, they combine with AND logic. Returns a list of <code>BookRecord</code> objects.</p>"},{"location":"python-api/#lookup","title":"Lookup","text":"<pre><code>book = catalog.get(1342)           # BookRecord or None\ncid = catalog.canonical_id(1342)   # canonical ID or None\n</code></pre> <p><code>canonical_id</code> resolves alternate edition IDs to the canonical one.</p>"},{"location":"python-api/#bookrecord","title":"BookRecord","text":"<p>A frozen dataclass with these fields:</p> Field Type Description <code>id</code> <code>int</code> Project Gutenberg ID <code>title</code> <code>str</code> Book title <code>authors</code> <code>str</code> Semicolon-separated author names <code>language</code> <code>str</code> Language code (e.g. <code>\"en\"</code>) <code>subjects</code> <code>str</code> Semicolon-separated subjects <code>locc</code> <code>str</code> Library of Congress Classification <code>bookshelves</code> <code>str</code> Gutenberg bookshelves <code>issued</code> <code>str</code> Publication date <code>type</code> <code>str</code> Media type (e.g. <code>\"Text\"</code>)"},{"location":"python-api/#catalog-policy","title":"Catalog policy","text":"<p>By default, <code>fetch()</code> applies a policy that keeps only English text and deduplicates by lowest ID per work. To customize:</p> <pre><code>from gutenbit.catalog import CatalogPolicy\n\npolicy = CatalogPolicy(\n    allowed_language_codes=frozenset({\"en\", \"fr\"}),\n    dedupe_strategy=\"none\",\n)\ncatalog = Catalog.fetch(policy=policy)\n</code></pre> <p>See the API Reference for full details on <code>CatalogPolicy</code>.</p>"},{"location":"python-api/#database","title":"Database","text":"<p><code>Database</code> wraps a SQLite file. Use it as a context manager:</p> <pre><code>from gutenbit import Database\n\nwith Database(\".gutenbit/gutenbit.db\") as db:\n    # all operations here\n    ...\n</code></pre> <p>Or manage the connection manually with <code>db.close()</code>.</p>"},{"location":"python-api/#ingest","title":"Ingest","text":"<pre><code>catalog = Catalog.fetch()\nbooks = catalog.search(author=\"Tolstoy\")\n\nwith Database(\".gutenbit/gutenbit.db\") as db:\n    db.ingest(books)\n</code></pre> <p><code>ingest</code> downloads each book's HTML from Project Gutenberg, parses it into chunks, and stores everything in the database. Books already present (at the current chunker version) are skipped unless you pass <code>force=True</code>.</p> <p>The <code>delay</code> parameter controls the pause between downloads. The default is 1 second, which is polite to Gutenberg's servers:</p> <pre><code>db.ingest(books, delay=2.0)\ndb.ingest(books, force=True)  # reprocess even if already current\n</code></pre>"},{"location":"python-api/#search_1","title":"Search","text":"<pre><code>results = db.search(\"battle\")\n</code></pre> <p>Returns a list of <code>SearchResult</code> objects ordered by BM25 rank by default.</p> <p>Filters narrow the result set:</p> <pre><code>results = db.search(\"battle\", author=\"Tolstoy\")\nresults = db.search(\"battle\", book_id=2600)\nresults = db.search(\"battle\", kind=\"text\")\nresults = db.search(\"battle\", title=\"War\")\n</code></pre> <p>Metadata filters (<code>author</code>, <code>title</code>, <code>language</code>, <code>subject</code>) use substring matching. <code>book_id</code> and <code>kind</code> are exact.</p> <p>Order controls result ordering:</p> <pre><code>db.search(\"battle\", order=\"rank\")    # BM25 score (default)\ndb.search(\"battle\", order=\"first\")   # book_id asc, position asc\ndb.search(\"battle\", order=\"last\")    # book_id desc, position desc\n</code></pre> <p>Limit controls the maximum number of results:</p> <pre><code>db.search(\"battle\", limit=50)  # default is 20\n</code></pre> <p>FTS5 query syntax is supported directly:</p> <pre><code>db.search('\"to be or not to be\"')         # exact phrase\ndb.search(\"war AND peace\")                 # boolean\ndb.search(\"war NOT peace\")                 # exclusion\ndb.search(\"philos*\")                       # prefix match\n</code></pre>"},{"location":"python-api/#searchresult","title":"SearchResult","text":"<p>A frozen dataclass with these fields:</p> Field Type Description <code>chunk_id</code> <code>int</code> Internal row ID <code>book_id</code> <code>int</code> Project Gutenberg ID <code>title</code> <code>str</code> Book title <code>authors</code> <code>str</code> Author names <code>language</code> <code>str</code> Language code <code>subjects</code> <code>str</code> Subjects <code>div1</code> <code>str</code> Broadest structural division <code>div2</code> <code>str</code> Second level <code>div3</code> <code>str</code> Third level <code>div4</code> <code>str</code> Deepest level <code>position</code> <code>int</code> Chunk index in document order <code>content</code> <code>str</code> Full text of the matching chunk <code>kind</code> <code>str</code> <code>\"heading\"</code> or <code>\"text\"</code> <code>char_count</code> <code>int</code> Character length of content <code>score</code> <code>float</code> BM25 relevance score (higher is better)"},{"location":"python-api/#reading-chunks","title":"Reading chunks","text":"<p>Several methods retrieve chunks without a search query.</p> <p>All chunks for a book:</p> <pre><code>records = db.chunk_records(1342)\nfor chunk in records:\n    print(chunk.position, chunk.div1, chunk.kind, chunk.content[:60])\n</code></pre> <p>Filter by kind:</p> <pre><code>headings = db.chunk_records(1342, kinds=[\"heading\"])\n</code></pre> <p>By position:</p> <pre><code>chunk = db.chunk_by_position(1342, position=50)\n</code></pre> <p>A window around a position:</p> <pre><code>window = db.chunk_window(1342, position=50, around=3)\n# Returns chunks at positions 47, 48, 49, 50, 51, 52, 53\n</code></pre> <p>The CLI <code>view --radius</code> and <code>search --radius</code> options use this same centered-window concept, but present it as a simple surrounding passage in reading order.</p> <p>By section path:</p> <pre><code>section = db.chunks_by_div(1342, \"Chapter 1\")\nsection = db.chunks_by_div(1342, \"BOOK ONE/CHAPTER I\", kinds=[\"text\"], limit=10)\n</code></pre> <p><code>chunks_by_div</code> matches by prefix on the div hierarchy. Matching ignores trailing punctuation and is case-insensitive.</p>"},{"location":"python-api/#chunkrecord","title":"ChunkRecord","text":"<p>A frozen dataclass with these fields:</p> Field Type Description <code>chunk_id</code> <code>int</code> Internal row ID <code>book_id</code> <code>int</code> Project Gutenberg ID <code>div1</code> <code>str</code> Broadest structural division <code>div2</code> <code>str</code> Second level <code>div3</code> <code>str</code> Third level <code>div4</code> <code>str</code> Deepest level <code>position</code> <code>int</code> Chunk index in document order <code>content</code> <code>str</code> Full text <code>kind</code> <code>str</code> Chunk kind <code>char_count</code> <code>int</code> Character length of content"},{"location":"python-api/#full-text","title":"Full text","text":"<pre><code>text = db.text(1342)\n</code></pre> <p>Returns the full reconstructed text (all chunks joined with double newlines), or <code>None</code> if the book is not stored.</p>"},{"location":"python-api/#book-management","title":"Book management","text":"<pre><code>all_books = db.books()             # list of BookRecord\nstale_books = db.stale_books()     # stored books that need reprocessing\nbook = db.book(1342)               # BookRecord or None\ndb.has_text(1342)                  # True if stored\ndb.has_current_text(1342)          # True if stored at current chunker version\ndb.remove_book(1342)               # returns True if removed, False if not found\n</code></pre>"},{"location":"python-api/#chunking-html-directly","title":"Chunking HTML directly","text":"<p>For advanced use, you can chunk HTML without the database:</p> <pre><code>from gutenbit import chunk_html\n\nhtml = open(\"book.html\").read()\nchunks = chunk_html(html)\n\nfor chunk in chunks[:10]:\n    print(chunk.position, chunk.div1, chunk.kind, chunk.content[:60])\n</code></pre>"},{"location":"python-api/#chunk","title":"Chunk","text":"<p>A frozen dataclass with these fields:</p> Field Type Description <code>position</code> <code>int</code> Chunk index in document order <code>div1</code> <code>str</code> Broadest structural division <code>div2</code> <code>str</code> Second level <code>div3</code> <code>str</code> Third level <code>div4</code> <code>str</code> Deepest level <code>content</code> <code>str</code> Text content <code>kind</code> <code>str</code> <code>\"heading\"</code> or <code>\"text\"</code> <p>See Concepts for how divisions and chunk kinds work.</p>"},{"location":"reference/","title":"API Reference","text":"<p>API pages are generated from source modules at build time.</p>"},{"location":"reference/SUMMARY/","title":"SUMMARY","text":"<ul> <li>Overview</li> <li>gutenbit<ul> <li>_cache</li> <li>_http</li> <li>_version</li> <li>catalog</li> <li>cli</li> <li>db</li> <li>display</li> <li>download</li> <li>html_chunker</li> </ul> </li> </ul>"},{"location":"reference/gutenbit/","title":"<code>gutenbit</code>","text":""},{"location":"reference/gutenbit/#gutenbit","title":"<code>gutenbit</code>","text":"<p>gutenbit \u2014 Download, parse, and store Project Gutenberg texts.</p>"},{"location":"reference/gutenbit/#gutenbit.BookRecord","title":"<code>BookRecord(id: int, title: str, authors: str, language: str, subjects: str, locc: str, bookshelves: str, issued: str, type: str)</code>  <code>dataclass</code>","text":"<p>A book entry from the Project Gutenberg catalog.</p>"},{"location":"reference/gutenbit/#gutenbit.Catalog","title":"<code>Catalog(records: list[BookRecord], *, canonical_id_by_id: dict[int, int] | None = None, fetch_info: CatalogFetchInfo | None = None)</code>","text":"<p>The Project Gutenberg catalog, searchable in memory.</p>"},{"location":"reference/gutenbit/#gutenbit.Catalog.canonical_id","title":"<code>canonical_id(book_id: int) -&gt; int | None</code>","text":"<p>Resolve any known id to the canonical id under current policy.</p>"},{"location":"reference/gutenbit/#gutenbit.Catalog.fetch","title":"<code>fetch(*, policy: CatalogPolicy = DEFAULT_CATALOG_POLICY, cache_dir: str | Path | None = None, refresh: bool = False) -&gt; Catalog</code>  <code>classmethod</code>","text":"<p>Download the CSV catalog from Project Gutenberg.</p>"},{"location":"reference/gutenbit/#gutenbit.Catalog.get","title":"<code>get(book_id: int) -&gt; BookRecord | None</code>","text":"<p>Return a canonical book record for a requested id.</p>"},{"location":"reference/gutenbit/#gutenbit.Catalog.is_canonical_id","title":"<code>is_canonical_id(book_id: int) -&gt; bool</code>","text":"<p>Return True when an id is already canonical under current policy.</p>"},{"location":"reference/gutenbit/#gutenbit.Catalog.search","title":"<code>search(*, author: str = '', title: str = '', language: str = '', subject: str = '') -&gt; list[BookRecord]</code>","text":"<p>Search for books matching all given criteria.</p> <p>All filters use case-insensitive matching. Each query is first tried as a contiguous substring; if it contains multiple words and the substring fails, every word must appear individually (so <code>\"Jane Austen\"</code> matches <code>\"Austen, Jane, 1775-1817\"</code>).</p>"},{"location":"reference/gutenbit/#gutenbit.Chunk","title":"<code>Chunk(position: int, div1: str, div2: str, div3: str, div4: str, content: str, kind: str)</code>  <code>dataclass</code>","text":"<p>A discrete block extracted from a book, labelled by kind.</p> <p>Structural divisions (div1\u2013div4) are compacted so the shallowest heading level always fills div1 first. For a chapter-only book, chapters go in div1; for a book with BOOK + CHAPTER, BOOK fills div1 and CHAPTER fills div2.</p> <p>Kinds: <code>\"heading\"</code>, <code>\"text\"</code></p>"},{"location":"reference/gutenbit/#gutenbit.Database","title":"<code>Database(path: str | Path)</code>","text":"<p>SQLite database for storing and searching Project Gutenberg books.</p>"},{"location":"reference/gutenbit/#gutenbit.Database.book","title":"<code>book(book_id: int) -&gt; BookRecord | None</code>","text":"<p>Return one stored book by Project Gutenberg id.</p>"},{"location":"reference/gutenbit/#gutenbit.Database.books","title":"<code>books() -&gt; list[BookRecord]</code>","text":"<p>Return all stored books.</p>"},{"location":"reference/gutenbit/#gutenbit.Database.chunk_by_id","title":"<code>chunk_by_id(book_id: int, chunk_id: int) -&gt; ChunkRecord | None</code>","text":"<p>Return one chunk by internal row id within a specific book.</p>"},{"location":"reference/gutenbit/#gutenbit.Database.chunk_by_position","title":"<code>chunk_by_position(book_id: int, position: int) -&gt; ChunkRecord | None</code>","text":"<p>Return one chunk by structural position within a specific book.</p>"},{"location":"reference/gutenbit/#gutenbit.Database.chunk_records","title":"<code>chunk_records(book_id: int, *, kinds: list[str] | None = None) -&gt; list[ChunkRecord]</code>","text":"<p>Return all chunks for a book as ChunkRecord objects.</p>"},{"location":"reference/gutenbit/#gutenbit.Database.chunk_window","title":"<code>chunk_window(book_id: int, position: int, *, around: int = 0) -&gt; list[ChunkRecord]</code>","text":"<p>Return the selected position and N neighboring chunks on each side.</p>"},{"location":"reference/gutenbit/#gutenbit.Database.chunks","title":"<code>chunks(book_id: int, *, kinds: list[str] | None = None) -&gt; list[tuple[int, str, str, str, str, str, str, int]]</code>","text":"<p>Return chunks as <code>(position, div1, div2, div3, div4, content, kind, char_count)</code>.</p>"},{"location":"reference/gutenbit/#gutenbit.Database.chunks_by_div","title":"<code>chunks_by_div(book_id: int, div_path: str, *, kinds: list[str] | None = None, limit: int = 0) -&gt; list[ChunkRecord]</code>","text":"<p>Return chunks under a division path prefix.</p> <p>Each segment is matched exactly, except that the deepest query segment also accepts a prefix match (so <code>\"CHAPTER I\"</code> matches <code>\"CHAPTER I DESCRIPTION OF A PALACE\"</code>).  Trailing punctuation is always ignored.</p>"},{"location":"reference/gutenbit/#gutenbit.Database.has_current_text","title":"<code>has_current_text(book_id: int) -&gt; bool</code>","text":"<p>Return True when stored text matches the current chunker version.</p>"},{"location":"reference/gutenbit/#gutenbit.Database.has_text","title":"<code>has_text(book_id: int) -&gt; bool</code>","text":"<p>Return True when a book has already been downloaded and stored.</p>"},{"location":"reference/gutenbit/#gutenbit.Database.ingest","title":"<code>ingest(books: list[BookRecord], *, delay: float = 1.0, force: bool = False) -&gt; None</code>","text":"<p>Download, chunk, and store books.</p> <p>Enforces package ingestion boundaries: English text records only, with in-request duplicate work IDs collapsed to a canonical edition.</p>"},{"location":"reference/gutenbit/#gutenbit.Database.remove_book","title":"<code>remove_book(book_id: int) -&gt; bool</code>","text":"<p>Remove a stored book and all associated rows. Returns False if missing.</p>"},{"location":"reference/gutenbit/#gutenbit.Database.search","title":"<code>search(query: str, *, author: str | None = None, title: str | None = None, language: str | None = None, subject: str | None = None, book_id: int | None = None, kind: str | None = None, div_path: str | None = None, order: SearchOrder = 'rank', limit: int = 20) -&gt; list[SearchResult]</code>","text":"<p>Search chunks via FTS5 with BM25 ranking.</p> <p>When div_path is given, results are post-filtered using the same path-prefix matching as :meth:<code>chunks_by_div</code> (normalized, with word-boundary prefix on the deepest segment).</p>"},{"location":"reference/gutenbit/#gutenbit.Database.search_count","title":"<code>search_count(query: str, *, author: str | None = None, title: str | None = None, language: str | None = None, subject: str | None = None, book_id: int | None = None, kind: str | None = None, div_path: str | None = None) -&gt; int</code>","text":"<p>Return the total number of search hits before any CLI display limit.</p>"},{"location":"reference/gutenbit/#gutenbit.Database.search_page","title":"<code>search_page(query: str, *, author: str | None = None, title: str | None = None, language: str | None = None, subject: str | None = None, book_id: int | None = None, kind: str | None = None, div_path: str | None = None, order: SearchOrder = 'rank', limit: int = 20) -&gt; SearchPage</code>","text":"<p>Return one CLI search page plus an exact total-hit count.</p>"},{"location":"reference/gutenbit/#gutenbit.Database.stale_books","title":"<code>stale_books() -&gt; list[BookRecord]</code>","text":"<p>Return stored books whose text is missing or stale for this chunker version.</p>"},{"location":"reference/gutenbit/#gutenbit.Database.text","title":"<code>text(book_id: int) -&gt; str | None</code>","text":"<p>Return the clean text for a book, or None if not found.</p>"},{"location":"reference/gutenbit/#gutenbit.Database.text_states","title":"<code>text_states(book_ids: list[int]) -&gt; dict[int, TextState]</code>","text":"<p>Return stored text presence/currentness for the requested ids.</p>"},{"location":"reference/gutenbit/#gutenbit.SearchResult","title":"<code>SearchResult(chunk_id: int, book_id: int, title: str, authors: str, language: str, subjects: str, div1: str, div2: str, div3: str, div4: str, position: int, content: str, kind: str, char_count: int, score: float)</code>  <code>dataclass</code>","text":"<p>A single search hit \u2014 one chunk with its book metadata.</p>"},{"location":"reference/gutenbit/#gutenbit.chunk_html","title":"<code>chunk_html(html: str) -&gt; list[Chunk]</code>","text":"<p>Split an HTML book into labelled chunks using TOC plus body heading cues.</p> <p>Each <code>&lt;p&gt;</code> element becomes its own chunk. Returns chunks in document order.</p>"},{"location":"reference/gutenbit/_cache/","title":"<code>gutenbit._cache</code>","text":""},{"location":"reference/gutenbit/_cache/#gutenbit._cache","title":"<code>_cache</code>","text":"<p>Shared cache helpers for Project Gutenberg data.</p>"},{"location":"reference/gutenbit/_cache/#gutenbit._cache.cache_age_seconds","title":"<code>cache_age_seconds(path: Path, *, now: float) -&gt; float | None</code>","text":"<p>Return cache age in seconds, or None when the file is unavailable.</p>"},{"location":"reference/gutenbit/_cache/#gutenbit._cache.default_cache_dir","title":"<code>default_cache_dir() -&gt; Path</code>","text":"<p>Return the default user cache directory for gutenbit.</p>"},{"location":"reference/gutenbit/_cache/#gutenbit._cache.read_cache_bytes","title":"<code>read_cache_bytes(path: Path) -&gt; bytes | None</code>","text":"<p>Read a cache payload, treating empty or unreadable files as missing.</p>"},{"location":"reference/gutenbit/_cache/#gutenbit._cache.write_bytes_atomic","title":"<code>write_bytes_atomic(path: Path, payload: bytes) -&gt; None</code>","text":"<p>Atomically replace a cache payload.</p>"},{"location":"reference/gutenbit/_http/","title":"<code>gutenbit._http</code>","text":""},{"location":"reference/gutenbit/_http/#gutenbit._http","title":"<code>_http</code>","text":"<p>Shared HTTP helpers for Project Gutenberg access.</p>"},{"location":"reference/gutenbit/_http/#gutenbit._http.gutenberg_request_headers","title":"<code>gutenberg_request_headers() -&gt; dict[str, str]</code>","text":"<p>Return identifying headers for Gutenberg and PGLAF requests.</p>"},{"location":"reference/gutenbit/_version/","title":"<code>gutenbit._version</code>","text":""},{"location":"reference/gutenbit/_version/#gutenbit._version","title":"<code>_version</code>","text":""},{"location":"reference/gutenbit/catalog/","title":"<code>gutenbit.catalog</code>","text":""},{"location":"reference/gutenbit/catalog/#gutenbit.catalog","title":"<code>catalog</code>","text":"<p>Fetch and search the Project Gutenberg CSV catalog.</p>"},{"location":"reference/gutenbit/catalog/#gutenbit.catalog.BookRecord","title":"<code>BookRecord(id: int, title: str, authors: str, language: str, subjects: str, locc: str, bookshelves: str, issued: str, type: str)</code>  <code>dataclass</code>","text":"<p>A book entry from the Project Gutenberg catalog.</p>"},{"location":"reference/gutenbit/catalog/#gutenbit.catalog.Catalog","title":"<code>Catalog(records: list[BookRecord], *, canonical_id_by_id: dict[int, int] | None = None, fetch_info: CatalogFetchInfo | None = None)</code>","text":"<p>The Project Gutenberg catalog, searchable in memory.</p>"},{"location":"reference/gutenbit/catalog/#gutenbit.catalog.Catalog.canonical_id","title":"<code>canonical_id(book_id: int) -&gt; int | None</code>","text":"<p>Resolve any known id to the canonical id under current policy.</p>"},{"location":"reference/gutenbit/catalog/#gutenbit.catalog.Catalog.fetch","title":"<code>fetch(*, policy: CatalogPolicy = DEFAULT_CATALOG_POLICY, cache_dir: str | Path | None = None, refresh: bool = False) -&gt; Catalog</code>  <code>classmethod</code>","text":"<p>Download the CSV catalog from Project Gutenberg.</p>"},{"location":"reference/gutenbit/catalog/#gutenbit.catalog.Catalog.get","title":"<code>get(book_id: int) -&gt; BookRecord | None</code>","text":"<p>Return a canonical book record for a requested id.</p>"},{"location":"reference/gutenbit/catalog/#gutenbit.catalog.Catalog.is_canonical_id","title":"<code>is_canonical_id(book_id: int) -&gt; bool</code>","text":"<p>Return True when an id is already canonical under current policy.</p>"},{"location":"reference/gutenbit/catalog/#gutenbit.catalog.Catalog.search","title":"<code>search(*, author: str = '', title: str = '', language: str = '', subject: str = '') -&gt; list[BookRecord]</code>","text":"<p>Search for books matching all given criteria.</p> <p>All filters use case-insensitive matching. Each query is first tried as a contiguous substring; if it contains multiple words and the substring fails, every word must appear individually (so <code>\"Jane Austen\"</code> matches <code>\"Austen, Jane, 1775-1817\"</code>).</p>"},{"location":"reference/gutenbit/catalog/#gutenbit.catalog.CatalogFetchInfo","title":"<code>CatalogFetchInfo(source: Literal['cache', 'downloaded', 'stale_cache'], cache_path: Path, cache_age_seconds: float | None = None)</code>  <code>dataclass</code>","text":"<p>How a catalog payload was loaded.</p>"},{"location":"reference/gutenbit/catalog/#gutenbit.catalog.CatalogPolicy","title":"<code>CatalogPolicy(allowed_language_codes: frozenset[str] = CATALOG_ALLOWED_LANGUAGE_CODES, allowed_media_types: frozenset[str] = CATALOG_ALLOWED_MEDIA_TYPES, dedupe_strategy: CatalogDedupeStrategy = CATALOG_DEDUPE_STRATEGY)</code>  <code>dataclass</code>","text":"<p>Config for catalog boundary enforcement.</p>"},{"location":"reference/gutenbit/catalog/#gutenbit.catalog.apply_catalog_policy","title":"<code>apply_catalog_policy(records: Iterable[BookRecord], *, policy: CatalogPolicy = DEFAULT_CATALOG_POLICY) -&gt; tuple[list[BookRecord], dict[int, int]]</code>","text":"<p>Filter and canonicalize records according to catalog policy.</p> <p>Returns a tuple of: 1. Canonical records after boundaries are applied. 2. Mapping from original record id -&gt; canonical record id.</p>"},{"location":"reference/gutenbit/catalog/#gutenbit.catalog.is_record_allowed","title":"<code>is_record_allowed(record: BookRecord, *, policy: CatalogPolicy = DEFAULT_CATALOG_POLICY) -&gt; bool</code>","text":"<p>Return True when a record is within the catalog policy.</p>"},{"location":"reference/gutenbit/catalog/#gutenbit.catalog.work_key","title":"<code>work_key(record: BookRecord) -&gt; tuple[str, str] | None</code>","text":"<p>Return a conservative key for canonical duplicate detection.</p>"},{"location":"reference/gutenbit/cli/","title":"<code>gutenbit.cli</code>","text":""},{"location":"reference/gutenbit/cli/#gutenbit.cli","title":"<code>cli</code>","text":"<p>Command-line interface for gutenbit.</p>"},{"location":"reference/gutenbit/db/","title":"<code>gutenbit.db</code>","text":""},{"location":"reference/gutenbit/db/#gutenbit.db","title":"<code>db</code>","text":"<p>SQLite storage and full-text search for Project Gutenberg books.</p>"},{"location":"reference/gutenbit/db/#gutenbit.db.ChunkRecord","title":"<code>ChunkRecord(chunk_id: int, book_id: int, div1: str, div2: str, div3: str, div4: str, position: int, content: str, kind: str, char_count: int)</code>  <code>dataclass</code>","text":"<p>One stored chunk with structural metadata.</p>"},{"location":"reference/gutenbit/db/#gutenbit.db.Database","title":"<code>Database(path: str | Path)</code>","text":"<p>SQLite database for storing and searching Project Gutenberg books.</p>"},{"location":"reference/gutenbit/db/#gutenbit.db.Database.book","title":"<code>book(book_id: int) -&gt; BookRecord | None</code>","text":"<p>Return one stored book by Project Gutenberg id.</p>"},{"location":"reference/gutenbit/db/#gutenbit.db.Database.books","title":"<code>books() -&gt; list[BookRecord]</code>","text":"<p>Return all stored books.</p>"},{"location":"reference/gutenbit/db/#gutenbit.db.Database.chunk_by_id","title":"<code>chunk_by_id(book_id: int, chunk_id: int) -&gt; ChunkRecord | None</code>","text":"<p>Return one chunk by internal row id within a specific book.</p>"},{"location":"reference/gutenbit/db/#gutenbit.db.Database.chunk_by_position","title":"<code>chunk_by_position(book_id: int, position: int) -&gt; ChunkRecord | None</code>","text":"<p>Return one chunk by structural position within a specific book.</p>"},{"location":"reference/gutenbit/db/#gutenbit.db.Database.chunk_records","title":"<code>chunk_records(book_id: int, *, kinds: list[str] | None = None) -&gt; list[ChunkRecord]</code>","text":"<p>Return all chunks for a book as ChunkRecord objects.</p>"},{"location":"reference/gutenbit/db/#gutenbit.db.Database.chunk_window","title":"<code>chunk_window(book_id: int, position: int, *, around: int = 0) -&gt; list[ChunkRecord]</code>","text":"<p>Return the selected position and N neighboring chunks on each side.</p>"},{"location":"reference/gutenbit/db/#gutenbit.db.Database.chunks","title":"<code>chunks(book_id: int, *, kinds: list[str] | None = None) -&gt; list[tuple[int, str, str, str, str, str, str, int]]</code>","text":"<p>Return chunks as <code>(position, div1, div2, div3, div4, content, kind, char_count)</code>.</p>"},{"location":"reference/gutenbit/db/#gutenbit.db.Database.chunks_by_div","title":"<code>chunks_by_div(book_id: int, div_path: str, *, kinds: list[str] | None = None, limit: int = 0) -&gt; list[ChunkRecord]</code>","text":"<p>Return chunks under a division path prefix.</p> <p>Each segment is matched exactly, except that the deepest query segment also accepts a prefix match (so <code>\"CHAPTER I\"</code> matches <code>\"CHAPTER I DESCRIPTION OF A PALACE\"</code>).  Trailing punctuation is always ignored.</p>"},{"location":"reference/gutenbit/db/#gutenbit.db.Database.has_current_text","title":"<code>has_current_text(book_id: int) -&gt; bool</code>","text":"<p>Return True when stored text matches the current chunker version.</p>"},{"location":"reference/gutenbit/db/#gutenbit.db.Database.has_text","title":"<code>has_text(book_id: int) -&gt; bool</code>","text":"<p>Return True when a book has already been downloaded and stored.</p>"},{"location":"reference/gutenbit/db/#gutenbit.db.Database.ingest","title":"<code>ingest(books: list[BookRecord], *, delay: float = 1.0, force: bool = False) -&gt; None</code>","text":"<p>Download, chunk, and store books.</p> <p>Enforces package ingestion boundaries: English text records only, with in-request duplicate work IDs collapsed to a canonical edition.</p>"},{"location":"reference/gutenbit/db/#gutenbit.db.Database.remove_book","title":"<code>remove_book(book_id: int) -&gt; bool</code>","text":"<p>Remove a stored book and all associated rows. Returns False if missing.</p>"},{"location":"reference/gutenbit/db/#gutenbit.db.Database.search","title":"<code>search(query: str, *, author: str | None = None, title: str | None = None, language: str | None = None, subject: str | None = None, book_id: int | None = None, kind: str | None = None, div_path: str | None = None, order: SearchOrder = 'rank', limit: int = 20) -&gt; list[SearchResult]</code>","text":"<p>Search chunks via FTS5 with BM25 ranking.</p> <p>When div_path is given, results are post-filtered using the same path-prefix matching as :meth:<code>chunks_by_div</code> (normalized, with word-boundary prefix on the deepest segment).</p>"},{"location":"reference/gutenbit/db/#gutenbit.db.Database.search_count","title":"<code>search_count(query: str, *, author: str | None = None, title: str | None = None, language: str | None = None, subject: str | None = None, book_id: int | None = None, kind: str | None = None, div_path: str | None = None) -&gt; int</code>","text":"<p>Return the total number of search hits before any CLI display limit.</p>"},{"location":"reference/gutenbit/db/#gutenbit.db.Database.search_page","title":"<code>search_page(query: str, *, author: str | None = None, title: str | None = None, language: str | None = None, subject: str | None = None, book_id: int | None = None, kind: str | None = None, div_path: str | None = None, order: SearchOrder = 'rank', limit: int = 20) -&gt; SearchPage</code>","text":"<p>Return one CLI search page plus an exact total-hit count.</p>"},{"location":"reference/gutenbit/db/#gutenbit.db.Database.stale_books","title":"<code>stale_books() -&gt; list[BookRecord]</code>","text":"<p>Return stored books whose text is missing or stale for this chunker version.</p>"},{"location":"reference/gutenbit/db/#gutenbit.db.Database.text","title":"<code>text(book_id: int) -&gt; str | None</code>","text":"<p>Return the clean text for a book, or None if not found.</p>"},{"location":"reference/gutenbit/db/#gutenbit.db.Database.text_states","title":"<code>text_states(book_ids: list[int]) -&gt; dict[int, TextState]</code>","text":"<p>Return stored text presence/currentness for the requested ids.</p>"},{"location":"reference/gutenbit/db/#gutenbit.db.SearchPage","title":"<code>SearchPage(items: list[SearchResult], total_results: int)</code>  <code>dataclass</code>","text":"<p>One CLI search page plus exact total-hit metadata.</p>"},{"location":"reference/gutenbit/db/#gutenbit.db.SearchResult","title":"<code>SearchResult(chunk_id: int, book_id: int, title: str, authors: str, language: str, subjects: str, div1: str, div2: str, div3: str, div4: str, position: int, content: str, kind: str, char_count: int, score: float)</code>  <code>dataclass</code>","text":"<p>A single search hit \u2014 one chunk with its book metadata.</p>"},{"location":"reference/gutenbit/db/#gutenbit.db.TextState","title":"<code>TextState(has_text: bool, has_current_text: bool)</code>  <code>dataclass</code>","text":"<p>Presence/currentness snapshot for one stored book.</p>"},{"location":"reference/gutenbit/display/","title":"<code>gutenbit.display</code>","text":""},{"location":"reference/gutenbit/display/#gutenbit.display","title":"<code>display</code>","text":"<p>Human-readable CLI rendering for gutenbit.</p>"},{"location":"reference/gutenbit/display/#gutenbit.display.CliDisplay","title":"<code>CliDisplay(stdout: TextIO = (lambda: sys.stdout)(), stderr: TextIO = (lambda: sys.stderr)(), interactive: bool | None = None, color: bool | None = None, width: int | None = None)</code>  <code>dataclass</code>","text":"<p>Render human-readable CLI output with TTY-aware styling.</p>"},{"location":"reference/gutenbit/download/","title":"<code>gutenbit.download</code>","text":""},{"location":"reference/gutenbit/download/#gutenbit.download","title":"<code>download</code>","text":"<p>Download Project Gutenberg HTML books.</p>"},{"location":"reference/gutenbit/download/#gutenbit.download.download_html","title":"<code>download_html(book_id: int) -&gt; str</code>","text":"<p>Download the HTML version of a book from Project Gutenberg.</p> <p>The resolver order is: 1. Generated HTML from aleph.pglaf.org. 2. Generated HTML from gutenberg.pglaf.org. 3. Main-site generated HTML zip.</p>"},{"location":"reference/gutenbit/html_chunker/","title":"<code>gutenbit.html_chunker</code>","text":""},{"location":"reference/gutenbit/html_chunker/#gutenbit.html_chunker","title":"<code>html_chunker</code>","text":"<p>HTML chunker for Project Gutenberg books.</p> <p>Uses the table of contents <code>&lt;a class=\"pginternal\"&gt;</code> links as the primary structural map. When a TOC is present but coarse, body headings can refine it without replacing TOC-derived hierarchy.</p> <p>Corpus boundaries are defined by Gutenberg's explicit text delimiters: <code>*** START OF (THE|THIS) PROJECT GUTENBERG EBOOK ... ***</code> through <code>*** END OF (THE|THIS) PROJECT GUTENBERG EBOOK ... ***</code>.</p> <p>Each <code>&lt;p&gt;</code> element becomes its own chunk \u2014 no accumulation or merging.</p>"},{"location":"reference/gutenbit/html_chunker/#gutenbit.html_chunker.Chunk","title":"<code>Chunk(position: int, div1: str, div2: str, div3: str, div4: str, content: str, kind: str)</code>  <code>dataclass</code>","text":"<p>A discrete block extracted from a book, labelled by kind.</p> <p>Structural divisions (div1\u2013div4) are compacted so the shallowest heading level always fills div1 first. For a chapter-only book, chapters go in div1; for a book with BOOK + CHAPTER, BOOK fills div1 and CHAPTER fills div2.</p> <p>Kinds: <code>\"heading\"</code>, <code>\"text\"</code></p>"},{"location":"reference/gutenbit/html_chunker/#gutenbit.html_chunker.chunk_html","title":"<code>chunk_html(html: str) -&gt; list[Chunk]</code>","text":"<p>Split an HTML book into labelled chunks using TOC plus body heading cues.</p> <p>Each <code>&lt;p&gt;</code> element becomes its own chunk. Returns chunks in document order.</p>"}]}